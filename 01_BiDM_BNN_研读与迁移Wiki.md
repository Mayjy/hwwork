# BiDM：从全二值扩散模型到现有 BNN 的可迁移方法

> 用途：论文研读、导师讨论、BNN 方案设计、早会汇报  
> 论文：*BiDM: Pushing the Limit of Quantization for Diffusion Models*（NeurIPS 2024）  
> 一句话结论：BiDM 的价值不只是“把扩散模型做到 W1A1”，而是证明了 BNN 的两个关键问题应分别处理——用动态缩放和跨步残差补足表示能力，用局部关系蒸馏降低离散优化难度。

## 1. 先说核心：BiDM 到底做了什么

全二值化把权重和激活都限制在 1 bit。卷积可以用 XNOR 与 bitcount 代替乘加，理论上非常省存储和计算，但扩散模型需要在许多去噪时间步中持续表达细微、动态的特征，普通 BNN 的固定 `sign` 量化会让生成结果直接崩溃。

BiDM 从两个互补方向解决问题：

1. **时间维度：Timestep-friendly Binary Structure（TBS）**
   - 长时间跨度上，不同 timestep 的激活范围差异很大，因此不能共用一个静态激活尺度。
   - 短时间跨度上，相邻 timestep 的高维特征又很相似，因此可以用邻近时间步的信息补偿当前二值特征。
   - TBS 因而包含两个部件：可学习的动态缩放卷积 `k`，以及跨 timestep 特征连接。

2. **空间维度：Space Patched Distillation（SPD）**
   - 全局逐点 L2 蒸馏要求容量很弱的二值学生精确拟合浮点教师，优化目标过硬、梯度容易被少数大误差位置主导。
   - 图像生成特征具有空间局部性。SPD 先切 patch，再在每个 patch 内匹配归一化的空间关系/注意力，使学生学习局部结构，而不是逐元素复制浮点数值。

可把整篇论文记成一句公式化表达：

`BiDM = 二值卷积基线 + 动态范围恢复 + 邻步特征补偿 + 局部关系蒸馏`

## 2. 为什么普通 BNN 在扩散模型里尤其困难

### 2.1 基线二值化

权重二值化为：

`w_b = σ · sign(w)`

其中 `σ` 是可学习浮点尺度；激活通常直接为：

`a_b = sign(a) ∈ {-1, +1}`

两者都为 1 bit 时，主体卷积可转换为 XNOR + bitcount。但 `sign(a)` 只保存符号，不保留幅值；对于随 timestep 改变分布的扩散激活，这种信息损失远比分类 BNN 更致命。

### 2.2 BiDM 的两条关键观察

**观察 1：时间上的“远异近同”。** 长期看激活范围随 timestep 明显变化；短期看相邻 timestep 的高维特征相似。前者要求动态量化尺度，后者允许跨步复用或融合。

**观察 2：空间上的“局部结构优先”。** 二值学生难以全局、逐点模仿浮点教师，但 U-Net 的卷积结构与图像生成任务具有局部性，局部关系比绝对值更适合作为蒸馏目标。

这两个观察比具体模块更重要：它们给出了一套设计 BNN 的方法——先识别任务结构中的条件变化与冗余，再据此恢复二值网络丢失的信息。

## 3. TBS：时间友好的二值结构

### 3.1 可学习动态激活缩放

XNOR-Net 不只对激活取符号，还用输入绝对值的通道均值 `A` 和小卷积核 `k` 形成空间尺度：

`I * W ≈ (sign(I) ⊗ sign(W)) ⊙ (A * k · α_w)`

- `⊗`：二值卷积；
- `A`：输入绝对值沿通道求平均后的二维图；
- `α_w`：权重尺度，可提前计算；
- `k`：原始 XNOR 中是固定均值滤波器，BiDM 把它改成可学习的 1×1×3×3 tiny convolution。

意义在于：尺度由当前输入生成，能自动随 timestep 和空间位置变化；而可学习 `k` 能校正“简单均值不能准确恢复局部激活范围”的问题。

论文强调，可学习 `k` 不增加相对于 XNOR 缩放路径的额外推理结构；它只是把原有固定卷积核变为训练得到的卷积核。仅把 `k` 设为可学习，LDM-4/LSUN-Bedrooms 的 FID 就从 106.62 降到 57.26（附录表 5）。

### 3.2 跨 timestep 特征连接

BiDM 在选定的高层 U-Net block 上，把当前 timestep 的特征与相邻 timestep 的对应特征加权融合：

`T_(t-1) = (1 - α) · L_(t-1) + α · L_t`

再送入当前上采样路径的 skip connection。`α` 按 block/timestep 独立学习，论文初始化为 0.3。直观上，当前二值特征方向不够准确，就从邻近时间步的相似高维特征中获得补偿。

这里不是 DeepCache 式“跳过计算”，而是“利用时间冗余增强表示”。代价也很明确：训练 `α` 时需要同时计算相邻 timestep，训练开销主要来自这里；推理仅保留少量浮点融合操作。

### 3.3 适用边界

跨 timestep 连接依赖三个条件：

- 模型确实是迭代式或序列式执行，并存在邻步特征相关性；
- 对应 block 在相邻步具有相同形状和语义；
- 系统能保存或获得邻步特征，且缓存开销可接受。

若你的 BNN 是普通单次前向分类网络，没有 timestep/state，这个部件不能直接照搬；应迁移为“相邻层、相邻帧、相邻迭代或 teacher-stage 的特征补偿”。

## 4. SPD：空间分块蒸馏

### 4.1 为什么不用普通 L2

浮点教师和二值学生的容量差距巨大。逐元素 L2 会强迫学生拟合不可达的浮点数值，并优先处理大误差像素，容易得到过度平滑、全局平均化的优化方向。论文对比中，普通 L2 蒸馏的 FID 为 26.07，SPD 为 22.74（附录表 7）。

### 4.2 SPD 的计算流程

对教师特征 `F_fp` 和学生特征 `F_bi`：

1. 在空间上切成 `p × p` 个对应 patch；
2. 对每个 patch 构造关系矩阵 `A = P · Pᵀ`；
3. 分别做 L2 归一化；
4. 对同一位置的教师/学生关系矩阵求 L2 差并平均；
5. 在多个 down/middle/up block 上求和，加到原扩散噪声预测损失中。

概念式为：

`L_total = L_DM + λ · mean_blocks(mean_patches(||normalize(A_fp) - normalize(A_bi)||_2))`

正文式 (16) 写默认 `λ=4`，但实验附录给出的实际任务设置是 CIFAR-10 用 `3e-2`，LSUN/FFHQ 用 `1e-2`。复现时应以具体实验设置为准，并把这一处差异记录在配置中。

### 4.3 SPD 真正学的是什么

SPD 不要求学生复制每个浮点激活，而是要求它在局部区域内保持相似的特征关系。对容量受限模型，关系蒸馏通常比数值蒸馏更可达；分 patch 又防止全局关系矩阵过大，并让损失更贴合卷积生成的局部性。

## 5. 实验结果应该怎样解读

### 5.1 主结果

在 LDM-4、LSUN-Bedrooms 256×256 上：

| 方法 | W/A bit | FID ↓ | sFID ↓ | Precision ↑ | Recall ↑ |
|---|---:|---:|---:|---:|---:|
| FP | 32/32 | 2.99 | 7.08 | 65.02 | 47.54 |
| XNOR | 1/1 | 106.62 | 56.81 | 6.82 | 5.22 |
| ReSTE | 1/1 | 59.44 | 42.16 | 12.06 | 2.92 |
| BiDM | 1/1 | 22.74 | 17.91 | 33.54 | 19.90 |

BiDM 并未接近浮点模型的绝对质量，但从“普通全二值基本失效”推进到“可生成可视图像”。论文报告相对浮点模型约 28.0× 存储节省、52.7× OPs 节省。

### 5.2 消融最能说明什么

| 方案 | FID ↓ | 解释 |
|---|---:|---|
| Vanilla W1A1 | 106.62 | 普通全二值扩散模型崩溃 |
| + TBS | 35.23 | 恢复动态范围与邻步信息，贡献最大 |
| + SPD | 40.62 | 更适合二值模型的局部关系优化 |
| TBS + SPD | 22.74 | 表示补偿与优化目标互补 |

这说明 BiDM 不是单个“神奇量化器”，而是同时处理了**前向表示误差**和**反向优化困难**。对你自己的 BNN，最合理的实验也应分别验证这两类改动。

### 5.3 不应过度宣称的地方

- 理论 OPs 节省不等于端到端真实延迟。论文在 Snapdragon 855 Plus 上测的是单个卷积，XNOR/BiDM 相对 FP 的实测约 8.07×，受部署库和硬件限制。
- 跨 timestep 连接显著增加训练时间，因为训练样本需同时做相邻步噪声估计；作者也把训练时间列为主要局限。
- 实验覆盖 DDIM/LDM 和几个图像数据集，不能直接推出对任意 BNN、任意任务同样有效。
- 浮点模型与 BiDM 之间仍有明显质量差距。

## 6. 怎样用到你自己的 BNN

由于当前没有你的代码和任务定义，建议按“通用优先、结构依赖后置”分三阶段迁移。

### 阶段 A：先迁移可学习动态缩放（最高优先级）

适用于几乎所有卷积 BNN：

1. 保留现有 `sign/STE` 权重与激活路径；
2. 从量化前激活计算 `mean(abs(x), channel)`；
3. 用可学习 depth-1 小卷积生成空间尺度；
4. 与权重尺度、二值卷积输出相乘；
5. 初始化 `k` 为均值滤波器，使新模型起点等价或接近 XNOR；
6. 先只训练/微调 `k` 与量化参数，确认数值稳定，再联合训练。

建议对比：原 BNN、静态标量缩放、输入动态均值缩放、可学习动态缩放。记录精度/任务指标、训练稳定性、延迟、FLOPs/BOPs 和额外浮点操作。

### 阶段 B：迁移 SPD（第二优先级）

如果有浮点 teacher，SPD 是低风险、训练期改动：

1. 选 2–4 个分辨率较大的中间 block，而不是所有层；
2. teacher 用 `eval()` 和 `no_grad()`；
3. teacher/student 特征形状不一致时加 projection，但不要把 projection 复杂度藏进结果；
4. 从 `p=2` 或 `p=4` 开始，保证 H、W 可整除；
5. 对关系矩阵加 `eps` 后归一化，防止零范数；
6. 对 `λ` 做对数尺度搜索，并与普通 feature L2 做公平对比。

若任务不是图像，可将“空间 patch”替换成语义局部单元：语音帧块、token window、图子结构或局部时间窗。

### 阶段 C：仅在存在结构冗余时迁移跨步补偿

- 扩散/迭代模型：使用相邻 denoising step；
- 视频 BNN：使用相邻帧或短期缓存；
- RNN/状态空间模型：使用相邻 state；
- 多阶段网络：尝试相邻 stage，但需先测相似度；
- 普通分类 CNN：不建议硬套 timestep 连接，可改做相邻层的 float shortcut/feature fusion。

落地前先画“block × step”的余弦相似度热图，只对相似度高且形状一致的 block 加连接。论文在 CIFAR-10 连接最后 2 个 timestep embedding blocks，在 LDM/LSUN/FFHQ 连接最后 8 个；这不是通用超参数。

## 7. 推荐实验矩阵

| 实验 | 动态 `k` | SPD | 跨步连接 | 目的 |
|---|:---:|:---:|:---:|---|
| E0 |  |  |  | 复现当前 BNN 基线 |
| E1 | ✓ |  |  | 验证范围恢复 |
| E2 |  | ✓ |  | 验证局部关系蒸馏 |
| E3 | ✓ | ✓ |  | 检验二者互补性 |
| E4 | ✓ | ✓ | ✓ | 仅对有 timestep/state 的模型 |

每个实验固定随机种子、训练预算与数据增强；同时报告主任务指标、参数/模型大小、BOPs、额外 FLOPs、峰值显存、训练时间和真实设备延迟。不要只报理论压缩比。

建议的停止条件：如果 E1/E2 在至少 3 个种子上没有稳定收益，先检查量化层位置、STE、归一化层和 teacher 对齐，不直接扩大模型或增加更多连接。

## 8. 早会 5 分钟讲稿

### 0:00–0:40：问题

“我读的是 NeurIPS 2024 的 BiDM。它研究的是扩散模型的极限量化：权重和激活都压到 1 bit。普通 W1A1 方法在扩散模型上几乎崩溃，因为 sign 只保留符号，而扩散过程的激活范围会随 timestep 变化，生成任务又需要丰富的局部细节。”

### 0:40–2:10：两个核心模块

“第一是 TBS。作者发现 timestep 特征远期差异大、近期相似。因此一方面把 XNOR 的固定均值卷积核改成可学习的小卷积，让激活尺度随输入动态变化；另一方面融合相邻 timestep 的高层特征，补偿二值表示。第二是 SPD。普通 L2 让二值学生逐点拟合浮点教师，目标太难；作者把中间特征分 patch，在每个局部 patch 内匹配归一化关系矩阵，让学生优先保持局部结构。”

### 2:10–3:10：结果

“在 LDM-4 的 LSUN-Bedrooms 上，普通 XNOR W1A1 的 FID 是 106.62，单独 TBS 是 35.23，单独 SPD 是 40.62，组合后是 22.74。它仍不如 FP 的 2.99，但已经从不可用推进到可生成图像，同时论文报告 28 倍存储和 52.7 倍理论 OPs 节省。”

### 3:10–4:30：对自己 BNN 的启发

“我认为最值得先迁移的不是跨 timestep 连接，而是两个更通用的点：第一，用输入相关、可学习的尺度恢复 sign 丢失的幅值；第二，用局部关系蒸馏替代全局逐点 L2。跨 timestep 连接只有在我们的网络存在迭代步、帧或状态冗余时再做。我计划先做 baseline、动态 k、SPD、二者组合四组消融。”

### 4:30–5:00：风险和下一步

“需要注意理论 BOPs 不等于端到端延迟，跨步连接也会明显增加训练时间。下一步我会先让 Agent 定位现有量化层和 feature hook，落地动态缩放与 SPD；拿到基线后再决定是否做跨步补偿。”

## 9. 导师可能追问的问题

**Q：BiDM 最核心的创新是哪个？**  
A：从消融看 TBS 单独收益略大，但真正的核心是“表示补偿 + 可达的蒸馏目标”两条线同时解决。两者组合优于任一单独模块。

**Q：可学习 `k` 为什么不会破坏二值加速？**  
A：主体卷积仍是二值 XNOR/bitcount；`k` 只作用在通道均值形成的单通道尺度图上，且原 XNOR 本来就有该缩放卷积。它增加的是少量浮点缩放路径，不把主卷积恢复成浮点。

**Q：为什么关系矩阵比 L2 好？**  
A：它放松了对绝对数值的要求，要求学生保持局部特征之间的相对结构，更符合低容量二值模型的可表示集合。

**Q：能否直接用于普通分类 BNN？**  
A：动态缩放可以直接试；SPD 可改为局部或通道关系蒸馏；跨 timestep 不能直接套用，除非模型存在相邻帧、迭代或状态。

## 10. 来源定位

- 方法总览与动机：论文第 1–2 页。
- BNN 基线、TBS：第 3–6 页，式 (6)–(12)。
- SPD：第 6–7 页，式 (13)–(16)。
- 主结果与消融：第 8–10 页，表 2–4。
- 训练配置、进一步消融与效率：附录第 16–19 页，表 5–9。

