# 部门洞察 Wiki：RoG——让大模型沿知识图谱“先规划、再检索、后推理”

> 论文：*Reasoning on Graphs: Faithful and Interpretable Large Language Model Reasoning*（ICLR 2024）  
> 适用主题：企业知识问答、减少幻觉、可解释推理、知识图谱与大模型融合  
> 一句话结论：RoG 不让 LLM 直接编造推理链，而是先生成由知识图谱关系构成的“路线图”，在图上执行得到真实路径，再让 LLM 从证据路径中筛选答案。

## 1. 洞察摘要

LLM 做复杂问答时有两个根本风险：知识可能过期或缺失，推理过程中还可能编造事实。知识图谱（KG）能提供结构化、可更新的事实，但常见 RAG 只把 KG 当作事实仓库，忽略了图的关系结构对多跳推理的指导作用。

RoG 的关键设计是把**关系路径**作为中间计划：

`问题 → 生成关系路径计划 → 在 KG 上受约束检索 → 得到实体级证据路径 → LLM 筛选并回答/解释`

例如问题“Who is the child of Alice?”，计划可以是：

`marry_to → father_of`

在图上执行后得到：

`Alice → marry_to → Bob → father_of → Charlie`

最后答案为 Charlie。计划只描述稳定的关系模式，具体实体事实则实时从 KG 获取，因此兼顾泛化、时效性和可解释性。

## 2. RoG 的三段式流程

### 2.1 Planning：生成关系路径

规划模块接收问题，生成 top-K 个关系序列：

`<PATH> relation_1 <SEP> relation_2 ... </PATH>`

训练监督来自 KG 中“问题实体到答案实体”的最短路径。也就是说，模型不是自由学习一段自然语言 CoT，而是学习 KG 中可执行的关系 schema。

这一设计的优势：

- 关系集合通常比实体集合小且稳定，较容易迁移；
- 计划可被图数据库执行，能够检查是否有效；
- 计划不绑定旧实体，KG 更新后仍可检索最新答案。

### 2.2 Retrieval：按计划在图上执行

以问题中识别出的实体为起点，使用受约束 BFS：第 `i` 跳只展开关系路径中规定的第 `i` 个关系。到达计划长度后，得到一组实体级 reasoning paths。

与无约束子图检索相比，它大幅缩小搜索空间，并保留完整证据链。论文使用 top-3 关系路径；K 增大虽提高答案覆盖率，也会增加噪声与检索时间。

### 2.3 Reasoning：从证据路径中筛选答案

检索路径可能包含多个候选甚至噪声，不能简单投票。推理模块把问题和结构化路径一起输入 LLM，让它识别相关路径、生成答案，并可按路径给出解释。

因此 Planning 与 Reasoning 缺一不可：Planning 负责找对证据空间，Reasoning 负责从候选证据中消歧和去噪。

## 3. 训练目标：同一个 LLM 学两项任务

RoG 用同一个 LLaMA2-Chat-7B 联合做两种 instruction tuning：

1. **规划优化**：最大化给定问题时生成 KG 有效关系路径的概率；监督信号是问题实体与答案实体之间的最短关系路径。
2. **检索-推理优化**：最大化给定问题与检索证据路径时生成正确答案的概率。

论文以 ELBO 解释该目标：规划项近似后验有效路径与模型路径分布之间的 KL；推理项最大化基于路径证据生成答案的概率。工程上可以更简单地理解为两个共享模型参数的序列生成任务。

训练数据包括 216,006 条 planning 数据、30,465 条 retrieval-reasoning 数据，以及 2,000 条解释数据。解释数据由每个数据集各采样 1,000 条，再借助 ChatGPT 生成解释。模型训练 3 个 epoch，batch size 4，学习率 `2e-5`，cosine scheduler，warmup ratio 0.03；2 张 A100-80G 训练约 38 小时。

## 4. 实验结果与证据

### 4.1 主结果

| 方法 | WebQSP Hits@1 | WebQSP F1 | CWQ Hits@1 | CWQ F1 |
|---|---:|---:|---:|---:|
| ChatGPT + CoT | 75.6 | - | 48.9 | - |
| UniKGQA | 77.2 | 72.2 | 51.2 | 49.1 |
| DECAF | 82.1 | 78.8 | - | - |
| RoG | **85.7** | 70.8 | **62.6** | **56.2** |

RoG 在大多数指标上最佳，但 WebQSP F1 低于 DECAF，汇报时不应笼统说“所有指标 SOTA”。它在更复杂、最多 4-hop 的 CWQ 上优势更明显。

### 4.2 消融揭示了什么

| 方案 | WebQSP F1 | CWQ F1 | 结论 |
|---|---:|---:|---|
| RoG | 70.81 | 56.17 | 完整系统 |
| 去掉 planning | 49.69 | 33.76 | 回到依赖模型内部知识，知识缺失严重 |
| 去掉 reasoning | 49.56 | 22.26 | 召回高但噪声大，不能把检索结果直接当答案 |
| random plans | 35.24 | 37.64 | 错误计划甚至可能比无计划更差 |
| vote reasoning | 47.96 | 26.52 | 多数投票不能替代语义消歧 |

核心洞察是：**高召回检索不等于高质量回答**。规划控制搜索方向，推理负责证据选择，两者形成精确率与召回率的平衡。

### 4.3 插件式能力

RoG 的 planning 模块可在不重新训练目标 LLM 的情况下，把检索到的路径作为上下文交给 ChatGPT、Alpaca、LLaMA2-Chat 或 Flan-T5。四类模型均获得明显提升。例如 ChatGPT 在 WebQSP 的 Hits@1 从 66.77 提升到 81.51，CWQ 从 39.90 提升到 52.68。

这对部门最有价值：不必先训练一个端到端大模型，可以先把“关系路径规划器 + 图检索”做成服务，为多个上层模型提供结构化证据。

## 5. 对部门业务的可迁移洞察

### 5.1 推荐的企业实现形态

`用户问题 → 实体链接 → 关系计划生成 → 图查询/受约束遍历 → 路径排序与裁剪 → LLM 基于证据回答 → 引用路径审计`

可以把关系路径翻译成 Cypher、SPARQL 或内部图查询 DSL。企业场景中还应加入权限过滤、时间有效性、来源可信度和审计日志。

### 5.2 最适合的场景

- 需要 2–4 跳关系推理的企业知识问答；
- 组织、人、项目、产品、缺陷、合同之间有明确关系的数据；
- 对幻觉敏感、需要展示证据链的风控、运维、研发知识场景；
- 知识频繁变化，但关系 schema 相对稳定的场景。

### 5.3 不一定适合的场景

- 答案主要存在于长文本语义，而不是结构化关系中；
- 实体链接质量差、图谱覆盖不足或关系 schema 混乱；
- 问题不需要多跳推理，普通检索即可解决；
- 强实时、超低延迟请求，无法承担多次生成和图遍历成本。

### 5.4 与普通 RAG 的关系

RoG 不是替代所有 RAG，而是补充“结构化多跳检索”。更实用的企业方案是混合检索：

- KG 路径负责结构化关系和可验证推理；
- 文档向量/BM25 负责非结构化细节；
- 最终回答同时引用图路径和原始文档。

## 6. 风险与论文局限

- 假设问题实体已经正确识别并链接到 KG；现实中的实体歧义会向后传播。
- 训练监督需要已知答案及可连接的 KG 路径，数据构造成本不低。
- 用最短路径作监督不保证它是唯一或语义最优的解释路径。
- top-K 增大会提高 recall，也会增加噪声、提示长度和检索时间；论文取 K=3 是数据集上的折中，不是通用最优值。
- 证据链来自 KG 只能说明“忠实于图谱”，不能保证图谱事实本身正确、完整或时效有效。
- 论文主要评估 Freebase 上的 WebQSP/CWQ；企业私有 KG 的 schema、数据质量和权限模型会显著改变效果。
- 解释由生成模型输出，仍需强制答案中的每个关键结论可回溯到具体三元组，不能只相信自然语言解释。

## 7. 部门 PoC 建议

### 第一阶段：不训练模型的最小验证

1. 选择 100–300 个真实多跳问题；
2. 建立小型业务 KG 和明确的关系字典；
3. 用现有 LLM few-shot 生成 top-3 关系路径；
4. 将路径编译成图查询并执行；
5. 把返回路径交给 LLM，只允许基于证据回答；
6. 人工评估路径有效率、答案正确率、证据忠实度和拒答质量。

### 第二阶段：做轻量规划器

从图中抽取“问题实体—答案实体”的有效/最短关系路径，构建 planning 训练集。可先用小模型或 LoRA，仅训练关系路径生成，不急于联合训练推理模块。

### 第三阶段：混合 RAG 与治理

加入文档检索、实体消歧、关系白名单、访问控制、路径数量上限、超时与无路径回退。上线指标应包括：答案准确率、路径执行成功率、引用覆盖率、幻觉率、P95 延迟、每问图查询量和 token 成本。

## 8. 早会 5 分钟讲稿

### 0:00–0:40：背景

“今天分享 ICLR 2024 的 RoG。它解决两个问题：大模型知识可能缺失或过期，同时自由生成 CoT 容易出现幻觉。知识图谱有可信事实和结构，但传统 RAG 往往只把它当事实库，没有利用关系路径来指导多跳推理。”

### 0:40–2:00：核心流程

“RoG 的方法是 planning、retrieval、reasoning 三步。先让模型生成关系路径，比如 marry_to 再 father_of；然后在 KG 上从问题实体出发，严格按这条关系路径做受约束 BFS，得到真实实体证据链；最后让 LLM 从多条路径中去噪并回答。这样自然语言模型负责语义规划和消歧，图数据库负责事实执行。”

### 2:00–3:10：结果

“在 WebQSP 上 RoG Hits@1 是 85.7，在更复杂的 CWQ 上是 62.6、F1 是 56.2。消融更有价值：去掉 planning，CWQ F1 从 56.17 降到 33.76；去掉 reasoning 降到 22.26，说明检索出来的路径不能直接投票，需要模型去噪。规划器还可以作为插件给不同 LLM 使用，不必重训目标模型。”

### 3:10–4:20：部门价值

“对部门业务，我认为最值得借鉴的是把 KG 的关系 schema 变成可执行计划，而不是把一堆三元组塞进提示词。可以先做一个规划与图检索服务，输出可审计的证据路径，再接不同模型。它特别适合组织、项目、产品、故障等实体关系明确的多跳问答。”

### 4:20–5:00：边界与建议

“边界是实体链接和图谱质量会决定上限，而且忠实于 KG 不代表 KG 本身绝对正确。我建议先用 100 到 300 个真实问题做不训练的 PoC，重点测路径有效率、答案正确率、证据忠实度和 P95 延迟，再决定是否训练专用规划器。”

## 9. 可能被问到的问题

**Q：它与 GraphRAG 有什么区别？**  
A：RoG 的论文核心是生成可执行的关系路径并受约束遍历，再由 LLM 推理；“GraphRAG”是更宽泛的图增强检索范式，具体实现可能包含社区摘要、子图检索或混合文档检索。不能简单画等号。

**Q：为什么不用 SPARQL 让 LLM 直接生成查询？**  
A：完整逻辑查询容易受语法和 schema 约束影响而不可执行；关系路径的表达更简单，先检索候选证据，再让 LLM 消歧，容错性更高。但复杂聚合、比较和约束问题仍可能需要查询语言。

**Q：解释是否真的可信？**  
A：证据路径可审计，但自然语言解释仍是生成的。工程上应把路径本身作为主要依据，并做“结论—三元组”对齐检查。

## 10. 来源定位

- 动机、总体框架：论文第 1–4 页。
- 联合优化、planning 与 retrieval-reasoning：第 4–6 页，式 (1)–(10)。
- 主结果、消融、插件式实验与案例：第 7–9 页，表 1–5。
- BFS 算法、数据构建与训练配置：附录第 16–19 页，算法 1、表 6–11。

